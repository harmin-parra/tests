{
  "timeout": 60000,
  "extension": ["ts"],
  "require": ["ts-node/register"],
  "recursive": true,
  "reporter": "allure-mocha",
  "reporterOptions": {
    "resultsDir" : "allure-results",
    "extraReporters": ["spec"]
  }
}
